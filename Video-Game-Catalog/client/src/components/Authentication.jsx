/* eslint-disable no-unused-vars */
import React from 'react'
import PropTypes from 'prop-types'

export const Authentication = props => {
  return (
    <div>Authentication Page</div>
  )
}

//Authentication.propTypes = {}  --> may be needed later

